// Replace this with your real notification logic
export async function sendInAppNotification(userId: string, title: string, body: string) {
  // Implement your real notification logic here (e.g., store in Civil Memory, push to a queue, etc.)
  console.log(`[NOTIFY] To: ${userId} | ${title} | ${body}`);
} 