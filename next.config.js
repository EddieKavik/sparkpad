/** @type {import('next').NextConfig} */
const nextConfig = {
  // Your Next.js configuration options can go here.
  // For example, to enable React's Strict Mode:
  reactStrictMode: true,
};

module.exports = nextConfig;
